# TODO

- Add the join-room dialog and move room list, room-id input, refresh, and join actions into it.
- Add the settings dialog and its settings actions.
- Add the help/rules dialog content.
- Add a server `profile/set_name` message if name changes must update players already in a room.
